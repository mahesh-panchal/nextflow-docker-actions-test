profiles {

    docker {
        docker.enabled = true
        docker.runOptions = '-u \$(id -u):\$(id -g)'
        docker.userEmulation = true
    }

    singularity {
        singularity.enabled = true
    }
}
