profiles {

    docker {
        docker.enabled = true
        // docker.runOptions = '-u \$(id -u):\$(id -g)'
        docker.userEmulation = true
        process.container = 'quay.io/biocontainers/adam:0.35.0--hdfd78af_0'
    }
}
